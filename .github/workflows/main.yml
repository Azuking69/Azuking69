name: GitHub Profile 3D Contrib

on:
  schedule:
    - cron: '0 0 * * *' # 毎日更新
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    name: generate 3d-contrib
    steps:
      - uses: actions/checkout@v3
      - uses: yoshi-s/github-profile-3d-contrib@v1
